
import 'package:flutter/material.dart';

class CreateMusic extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return Expanded(
      child: ListView(
        padding: EdgeInsets.all(0),
        children: <Widget>[
        Container(child: Center(child: Text('MUSIC')))
      ],)
    );
  }
}